import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farmerenterprise.app',
  appName: 'Velnexa Crop Solutin',
  webDir: 'dist/agroproductmanagementsystem/browser'
};

export default config;
